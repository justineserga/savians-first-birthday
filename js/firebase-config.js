// Fill these in with your own Firebase project's web config, found at:
// Firebase Console > Project settings > General > Your apps > Web app > SDK setup and configuration
//
// These values are meant to be public in client-side code — Firebase Web apps
// are secured with Firestore Security Rules, not by hiding this config.
// See README.md for the full setup steps (create project, enable Firestore,
// paste the rules, then fill this in).
//
// Until you fill these in, Birthday Flappy still works fully offline —
// the leaderboard panel will just show a "not connected yet" message.
window.SAVIAN_FIREBASE_CONFIG = {
  apiKey: 'YOUR_API_KEY',
  authDomain: 'YOUR_PROJECT_ID.firebaseapp.com',
  projectId: 'YOUR_PROJECT_ID',
  storageBucket: 'YOUR_PROJECT_ID.appspot.com',
  messagingSenderId: 'YOUR_SENDER_ID',
  appId: 'YOUR_APP_ID'
};
